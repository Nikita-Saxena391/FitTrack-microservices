spring:
  application:
    name: ai-service
  cloud:
    config:
      enabled: true
      allow-override: true
      override-none: false
      override-system-properties: false
      import: optional:configserver:http://localhost:8888
